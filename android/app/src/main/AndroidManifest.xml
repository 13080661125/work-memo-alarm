<?xml version="1.0" encoding="utf-8"?>
<!--
  =========================================================
  Android 清单文件：闹钟能否"后台准时响"，一大半取决于这里的权限
  =========================================================
  各权限作用说明见下方注释，复制本文件覆盖 android/app/src/main/AndroidManifest.xml 即可。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 通知权限：Android 13（API 33）起必须运行时申请，否则通知不弹出 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 精确闹钟：Android 12（API 31）起默认关闭，需在系统「闹钟和提醒」中手动打开。
         不开启时系统只允许"不精确"闹钟，可能被延迟数分钟。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Android 13+ 的精确闹钟权限：系统自动授予（Google Play 对使用范围有限制，
         闹钟/日历类应用属于合规场景）。声明它可以减少用户手动授权的概率。 -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 全屏意图：锁屏状态下直接弹出 APP 的响铃页面（闹钟场景必需） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 唤醒 CPU：保证响铃与播放铃声时不被立即休眠 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 开机/更新后恢复闹钟：不声明的话手机重启后所有闹钟都会失效 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 申请加入电池优化白名单（permission_handler 使用） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:label="工作备忘录"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true"
            android:showWhenLocked="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true">
            <!-- 上面三个属性：允许在锁屏上显示并点亮屏幕，是全屏闹钟弹窗的前提 -->

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="false" />

        <!-- ===== flutter_local_notifications 需要的广播接收器 ===== -->
        <!-- 定时通知的接收器 -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- 开机/应用更新后重新注册通知的接收器（重启后闹钟不失效的关键） -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 通知上的操作按钮（关闭 / 贪睡） -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
    </application>
</manifest>
